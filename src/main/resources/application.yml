spring:
  datasource:
    url: ${CSYE6225_SQL_DB_CONNSTR}
    username: ${CSYE6225_SQL_DB_USER}
    password: ${CSYE6225_SQL_DB_PWD}
    driverClassName: com.mysql.cj.jdbc.Driver
  mvc:
    converters:
      preferred-json-mapper: gson
  cloud:
    gcp:
      project-id: ${CSYE6225_PROJECT_ID}
server:
  port: 8080